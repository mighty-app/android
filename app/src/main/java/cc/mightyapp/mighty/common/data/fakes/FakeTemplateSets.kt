package cc.mightyapp.mighty.common.data.fakes

import cc.mightyapp.mighty.common.data.models.Routine
import cc.mightyapp.mighty.common.data.models.TemplateSet

object FakeTemplateSets {
    private val fakeRoutineStrongLiftsA: Routine = FakeRoutines.StrongLiftsA

    val StrongLiftsASquatOne = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.Squat
    )

    val StrongLiftsASquatTwo = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.Squat
    )

    val StrongLiftsASquatThree = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.Squat
    )

    val StrongLiftsASquatFour = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.Squat
    )

    val StrongLiftsASquatFive = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.Squat
    )

    val StrongLiftsABenchPressOne = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BenchPress
    )

    val StrongLiftsABenchPressTwo = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BenchPress
    )

    val StrongLiftsABenchPressThree = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BenchPress
    )

    val StrongLiftsABenchPressFour = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BenchPress
    )

    val StrongLiftsABenchPressFive = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BenchPress
    )

    val StrongLiftsABarbellRowOne = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BarbellRow
    )

    val StrongLiftsABarbellRowTwo = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BarbellRow
    )

    val StrongLiftsABarbellRowThree = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BarbellRow
    )

    val StrongLiftsABarbellRowFour = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BarbellRow
    )

    val StrongLiftsABarbellRowFive = TemplateSet(
        reps = 5,
        routine = fakeRoutineStrongLiftsA,
        exercise = FakeExercises.BarbellRow
    )
}